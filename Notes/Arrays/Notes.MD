# ARRAYS

## Finding the length of an array:
    '

    arrayName.length

## Declaring an array.

-Syntax:
    '

    dataType[] arrayName;

    arrayName = new dataType[arraySize];
    //creates a new array
    //assigns ref. of newly created array to arrayName
    //basically creating an array and giving it a type and size

    double[] myList = new double[10];
    //creates new array myList, of 10 elements of type double

## Array Methods

- A block of code performing a specific task is known as a method.

-So why methods?

1.Allows one to reuse code.

2.Simplifies debugging.

3.Code organization.

Method definition:

    accessModifier returnType methodName